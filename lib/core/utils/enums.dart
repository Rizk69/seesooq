enum AllTypes { dark,light }

enum SubscriptionStatus { active, blocked, pendingPayment, expired }

enum OfferStatus { accepted, completed }

enum WalletTransactionType { credit, debit }

enum OrderStatus {
  pending,
  confirmed,
  outForDelivery,
  completed,
  cancelled,
  refunded
}

enum DiscountTypes {
  fixed,
  percentage,
}
